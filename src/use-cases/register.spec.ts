import { expect, test } from 'vitest'

test('check if it works', () => {
  // This is a placeholder test to ensure the test suite runs
  // You can add your actual tests here
  expect(2 + 2).toBe(4)
})
